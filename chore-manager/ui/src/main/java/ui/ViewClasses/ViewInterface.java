package ui.ViewClasses;

import javafx.scene.Node;

/**
 * The ViewInterface interface is used to make sure that all views have a getFxml() method.
 */
public interface ViewInterface {
    public Node getFxml();
}
